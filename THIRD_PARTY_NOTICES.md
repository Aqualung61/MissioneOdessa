# Third-Party Notices

Questo file elenca **componenti di terze parti** usati dal progetto e riferimenti alle rispettive licenze.

Nota: l’elenco completo (incluse dipendenze transitive) dipende dall’installazione npm. Per una pubblicazione “formale”, genera un report licenze dal tuo ambiente CI/build.

## Runtime dependencies (npm)

Vedi `package.json` per versioni e sorgente di installazione.

- `express`
- `helmet`
- `express-rate-limit`
- `cors`
- `dotenv`

## Dev dependencies (npm)

- `vitest`
- `typescript`
- `eslint` + ecosistema TypeScript ESLint
- `prettier`

## Vendored / static assets

- `marked` (file: `web/js/marked.min.js`) — include header con licenza e repository upstream.

