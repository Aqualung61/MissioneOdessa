{
  "extends": "./tsconfig.json",
  "include": ["src/**/*.ts", "types/**/*.d.ts"],
  // Escludiamo intere directory di test (sia root che sotto src)
  "exclude": ["tests", "src/tests", "node_modules", "dist"]
}
