{
  "extends": "./tsconfig.json",
  "include": ["src/**/*.ts", "types/**/*.d.ts"],
  // Escludiamo ricorsivamente i test (sia root che sotto src)
  "exclude": ["tests/**/*", "src/tests/**/*", "node_modules", "dist"]
}
