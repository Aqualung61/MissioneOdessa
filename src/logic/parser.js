// Parser di comandi utente (placeholder, da completare secondo logica applicativa)
export function parseCommand(input) {
  // Esempio: ritorna il comando in maiuscolo
  return input.trim().toUpperCase();
}
